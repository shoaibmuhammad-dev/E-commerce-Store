import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <main className="w-full min-h-screen relative flex flex-col items-center justify-center px-4 bg-(--secondary-bg)">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
